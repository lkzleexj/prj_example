package com.tarena.demo;

public class Demo26
{
	/*  13
	 * 一个整数，它加上100后是一个完全平方数，再加上168又是一个
	 * 完全平方数，请问该数是多少？
	 */
	public static void main(String[] args)
	{
		int num;
		num+100==Math.pow(a, 2);
		Math.pow(a, 2)+168=Math.pow(b, 2);
				
	}
}
